// consultation-backend/routes/chat.js
const express = require('express');
const router = express.Router();

// Route placeholder for chat-related API endpoints
router.get('/', (req, res) => {
  res.json({ message: 'Chat route is working' });
});

module.exports = router;